- threesum, code constant space solution using twosum and pointers
    -- write testing for twosums
    -- write threesum
    -- watch video @10 min


- Configure pylint (rcfile?)
- Configure VIM
    -> ALE, black, isort, pylint, flake8
        -- configure black (read github)
            -> remove unused imports automatically
        -- pylint allow vars with 1 char name
    -> coc.nvim
        language server?
